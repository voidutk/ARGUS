# ARGUS intelligence service — required.
#
# These four are what the service actually needs to do its job: serve HTTP,
# validate payloads, and write the Neo4j projection.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
neo4j>=5.26

# The NER tier. Optional in the sense that extract.py imports it on demand and
# reports spacy_loaded:false rather than failing when it is absent — the regex
# tier finds every identifier the correlation engine links on, so the service
# stays useful without it. Pinned to the 3.8 line because en_core_web_sm 3.8.0
# is built against it; a model and a runtime from different minor lines will
# load and then quietly mis-tag.
#
# The model itself is NOT a pip requirement — install it separately:
#   python -m spacy download en_core_web_sm
spacy>=3.8,<3.9
